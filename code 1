import 'package:flutter/material.dart';

void main() {
  runApp(const ViberChatApp());
}

class ViberChatApp extends StatelessWidget {
  const ViberChatApp({super.key});

  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      debugShowCheckedModeBanner: false,
      title: 'ViberChat',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      home: const AccueilScreen(),
    );
  }
}

class AccueilScreen extends StatelessWidget {
  const AccueilScreen({super.key});

  void _entrerEnModeInvite(BuildContext context) {
    String idInvite = 'Invite_${DateTime.now().millisecondsSinceEpoch.toString().substring(8)}';
    
    ScaffoldMessenger.of(context).showSnackBar(
      SnackBar(
        content: Text('Mode invité activé ! Votre identifiant : $idInvite'),
        duration: const Duration(seconds: 3),
      ),
    );
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
      body: Center(
        child: Padding(
          padding: const EdgeInsets.all(24.0),
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children: [
              const Text(
                'ViberChat',
                style: TextStyle(
                  fontSize: 36,
                  fontWeight: FontWeight.bold,
                  color: Colors.blueAccent,
                ),
              ),
              const SizedBox(height: 20),
              const Text(
                'Bienvenue sur ton application',
                style: TextStyle(fontSize: 16, color: Colors.grey),
                textAlign: TextAlign.center,
              ),
              const SizedBox(height: 40),
              ElevatedButton(
                onPressed: () {
                  // Action pour la création de compte classique
                },
                child: const Text('Commencer / Créer un compte'),
              ),
              const SizedBox(height: 15),
              OutlinedButton(
                onPressed: () => _entrerEnModeInvite(context),
                child: const Text('Mode Invité (Regarder sans compte)'),
              ),
            ],
          ),
        ),
      ),
    );
  }
}

class RechercheAmisScreen extends StatefulWidget {
  const RechercheAmisScreen({super.key});

  @override
  State<RechercheAmisScreen> createState() => _RechercheAmisScreenState();
}

class _RechercheAmisScreenState extends State<RechercheAmisScreen> {
  final TextEditingController _rechercheController = TextEditingController();
  
  // Liste fictive d'utilisateurs pour simuler la recherche par nom et prénom
  final List<Map<String, String>> _utilisateurs = [
    {'nom': 'Mya', 'username': 'mya_off', 'photo': 'assets/mya.png'},
    {'nom': 'Noé', 'username': 'noe_t', 'photo': 'assets/noe.png'},
    {'nom': 'Thomas', 'username': 'thomas_92', 'photo': 'assets/thomas.png'},
  ];

  List<Map<String, String>> _resultatsFiltres = [];

  @override
  void initState() {
    super.initState();
    _resultatsFiltres = _utilisateurs;
  }

  void _rechercher(String query) {
    setState(() {
      _resultatsFiltres = _utilisateurs
          .where((user) => user['nom']!.toLowerCase().contains(query.toLowerCase()) ||
                           user['username']!.toLowerCase().contains(query.toLowerCase()))
          .toList();
    });
  }

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Ajouter un ami (Style Snapchat)'),
      ),
      body: Padding(
        padding: const EdgeInsets.all(16.0),
        child: Column(
          children: [
            TextField(
              controller: _rechercheController,
              onChanged: _rechercher,
              decoration: const InputDecoration(
                labelText: 'Rechercher par nom et prénom',
                prefixIcon: Icon(Icons.search),
                border: OutlineInputBorder(),
              ),
            ),
            const SizedBox(height: 20),
            Expanded(
              child: ListView.builder(
                itemCount: _resultatsFiltres.length,
                itemBuilder: (context, index) {
                  final user = _resultatsFiltres[index];
                  return ListTile(
                    leading: const CircleAvatar(
                      child: Icon(Icons.person),
                    ),
                    title: Text(user['nom']!),
                    subtitle: Text('@${user['username']}'),
                    trailing: ElevatedButton(
                      onPressed: () {
                        // Action pour ajouter l'ami
                      },
                      child: const Text('Ajouter'),
                    ),
                  );
                },
              ),
            ),
          ],
        ),
      ),
    );
  }
}
